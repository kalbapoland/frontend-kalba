{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Launch iOS Simulator",
      "type": "node-terminal",
      "request": "launch",
      "command": "npm run ios:dev",
      "cwd": "${workspaceFolder}"
    }
  ]
}
